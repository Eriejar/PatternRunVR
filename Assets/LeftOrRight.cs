using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LeftOrRight
{
    public string GetRelativeDirection(GameObject side, GameObject object1, GameObject object2)
    {
        // Returns direction of what object2 is in relation to object1 (if object 2 is on left of object 1 (pivot), returns "left")
        object1.GetComponents()


    }
}
